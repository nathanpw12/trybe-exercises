//------------------------------------------------------------------//

// Uma função pode receber parameros: string, number, object , bool, array , undefined

// Uma função é tratada como dado , por isso podemos guarda-las dentro de uma varíavel e dessa  mesma forma também podemos passar uma funçao para uma função como parametro

// o addEventListener é uma HOF nativa do Js, esse tipo de função pode ser chamado de metódo

//------------------------------------------------------------------//

// Tipos de função

// Tradicional:

function name(params) {
  
}

// Anônima:

const teste = function (params) {
  
}

// Arrow functions:

const arrow = () => {

}

// as arrow functions sãp tambem funcoes anonimas pois não tem nome

